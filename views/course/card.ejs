
<div 
    class="col col-sm-12 col-md-6 course-menu-card" 
    id="<%=course.id%>" 
    data-enable-lessons="<%= course.enableLessons %>"
>
    <div 
        class="card card-cover h-100 overflow-hidden text-bg-dark rounded-3 shadow-lg menu-card mb-3" 
        style="background-image: url('<%=course.imageURL%>');"
    >
        <% if (locals.admin_view) { %>
            <div class="d-grid gap-2 d-md-flex justify-content-md-end m-3">
                <button 
                    class="btn btn-success me-md-2" 
                    type="button"
                    data-bs-toggle="modal" 
                    data-bs-target="#course-modal"
                    role="put"
                >
                    Edit
                </button>
                <button 
                    class="btn btn-danger" 
                    type="button"
                    data-bs-toggle="modal" 
                    data-bs-target="#verify-delete-modal"
                    data-show-delete-func="delete_course_verification"
                >
                    Delete
                </button>
            </div>
        <% } %>
        <div 
            class="d-flex flex-column h-100 p-5 pb-4 text-white text-shadow-1" 
            onclick="location.href='/courses/<%=course.id%>'"
        >
            <h3 class="pt-5 mt-4 mb-4 display-6 lh-1 fw-bold text-capitalize course-title">
                <%=course.name%>
            </h3>
        </div>
    </div>
</div>