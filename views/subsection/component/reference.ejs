<% const uuid = Math.ceil(Math.random() * 2 ** 32)%>

<div id="ref-msg-<%= uuid %>" class="w-100 mr-1" style="display: inline;"></div>
<script type="module">
    new Quill("#ref-msg-<%= uuid %>", {
        modules: {},
        readOnly: true
    }).setContents(
        JSON.parse(String.raw`<%- component.reference.message %>`)
    );
</script>
<% if (component.reference.subsectionId) { %>
    <a href="/subsections/<%= component.reference.subsectionId %>" class="position-absolute top-0 start-0 p-1">
<% } else { %>
    <a href="/sections/<%= component.reference.sectionId %>" class="position-absolute top-0 start-0 p-1">
<% } %>
    <i class="bi bi-box-arrow-up-right" ></i>
</a>